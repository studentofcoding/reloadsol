user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
  worker_connections 1024;
}

http {
  include /etc/nginx/mime.types;
  default_type application/octet-stream;

  log_format main '$remote_addr - $request '
                  '"$status" cache=$upstream_cache_status '
                  'rt=$request_time u="$request_uri"';

  access_log /var/log/nginx/access.log main;

  sendfile on;
  tcp_nopush on;
  keepalive_timeout 65;
  gzip on;
  gzip_types text/plain text/css application/json application/javascript text/xml;

  # Docker embedded DNS — re-resolve upstream after container recreate (avoids stale IP → 502).
  resolver 127.0.0.11 valid=10s ipv6=off;
  resolver_timeout 5s;

  proxy_cache_path /var/cache/nginx/reloadsol
    levels=1:2
    keys_zone=reloadsol_cache:32m
    max_size=256m
    inactive=10m
    use_temp_path=off;

  include /etc/nginx/conf.d/*.conf;
}
