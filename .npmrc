# Use the official registry. Tencent Cloud's default mirror (mirrors.tencentyun.com)
# returns HTTP 451 for some packages (e.g. xrpl) due to regional restrictions.
registry=https://registry.npmjs.org/
legacy-peer-deps=true
fund=false
